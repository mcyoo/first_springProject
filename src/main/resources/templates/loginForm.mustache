{{>layout/header}}

<section class="flex justify-center items-center h-screen bg-gray-100">
    <div class="max-w-md w-full bg-white rounded p-6 space-y-4">
        <div class="mb-4">
            <p class="text-gray-600">Sign In</p>
            <h2 class="text-xl font-bold">Join our community</h2>
        </div>
        <div class="flex flex-col space-y-4">
            <a href="/oauth2/authorization/google" class="w-full py-4 bg-blue-600 hover:bg-blue-700 rounded text-sm font-bold text-gray-50 transition duration-200 text-center">구글 로그인</a>
            <a href="/oauth2/authorization/naver" class="w-full py-4 bg-blue-600 hover:bg-blue-700 rounded text-sm font-bold text-gray-50 transition duration-200 text-center">네이버 로그인</a>
        </div>
    </div>
</section>

{{>layout/footer}}